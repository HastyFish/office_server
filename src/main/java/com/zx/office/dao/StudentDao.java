package com.zx.office.dao;import com.zx.office.entity.Student;import org.apache.ibatis.annotations.Insert;import org.apache.ibatis.annotations.Select;import java.util.List;public interface StudentDao {    @Insert({"insert into student (student_id, student_enrollment, student_name, student_sex, clazz_id)  " +            "values " +            "(#{studentId}, #{studentEnrollment}, #{studentName}, #{studentSex}, #{clazzId})"})    int addStudent(Student student);    List<Student> selectAllStudent(String clazzId);    //查询学生Id    @Select({"select right(student_id,2) from student where clazz_id = #{clazzId} "})    List<String> selectSid(String clazzId);}